{"cells":[{"kind":1,"language":"markdown","value":"## This is a vscode notebook for the Fefl language\r\n\r\nIn the following we will show how to calculate the average of two number:"},{"kind":2,"language":"fefl","value":"let min = 14;\nlet max = 22;\n\nconst myConstant = 13;\nprint myConstant;\n\n// Can reassign an existing variable\nmin = 5;\n\n// Printing\nprint average(min, max);\nprint average(1, 3);\nprint average(2, 42);\n\nfunction average(min: number, max: number): number {\n    return (min + max) / 2;\n}"},{"kind":2,"language":"fefl","value":"function fibonacci(n: number): number {\n  if (n <= 1) {\n    return n;\n  } else {\n    return fibonacci(n - 1) + fibonacci(n - 2);\n  }\n}\n\n// Example usage:\nconst numTerms = 10; // Change this to get a different number of terms\n\nprint \"Fibonacci Sequence (first ${numTerms} terms):\";\nfor (let i = 0; i < numTerms; i = i + 1) {\n  print fibonacci(i);\n}"},{"kind":2,"language":"fefl","value":""}]}